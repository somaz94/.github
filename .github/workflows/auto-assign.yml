name: Auto Assign

# This repo dogfoods its own reusable auto-assign.
on:
  pull_request_target:
    types: [opened, reopened]

permissions:
  pull-requests: write

jobs:
  assign:
    uses: somaz94/.github/.github/workflows/auto-assign-reusable.yml@main
